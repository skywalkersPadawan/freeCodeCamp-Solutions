function getMonsterAttackValue(level) {
  const hit = level * 5 - Math.floor(Math.random() * xp);
  // use an extra set of brackets for the level expression and the math.floor expression to pass this challenge
}
